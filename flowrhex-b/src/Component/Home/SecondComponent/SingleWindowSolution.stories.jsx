
import React from 'react';
import SingleWindowsSolutions from './SingleWindowSolution'; // Adjust the path if necessary

export default {
  title: 'Components/SingleWindowsSolutions',
  component: SingleWindowsSolutions,
};

const Template = (args) => <SingleWindowsSolutions {...args} />;

export const Default = Template.bind({});
Default.args = {};
